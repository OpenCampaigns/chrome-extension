name: Build and Release

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  release:
    types: [published]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Checkout SDK
      uses: actions/checkout@v4
      with:
        repository: ${{ github.repository_owner }}/sdk
        path: sdk

    - name: Checkout Display
      uses: actions/checkout@v4
      with:
        repository: ${{ github.repository_owner }}/display
        path: display

    - name: Use Node.js 20.x
      uses: actions/setup-node@v4
      with:
        node-version: 20

    - name: Fix Local Dependencies
      run: sed -i 's|file:\.\./|file:./|g' package.json

    - name: Install dependencies
      run: npm install

    - name: Run Tests
      run: npm test

    - name: Build Extension
      run: npm run build

    - name: Package Extension
      run: |
        mkdir -p release
        cp -r dist/* release/
        cp -r public/* release/
        cd release && zip -r ../opencampaigns-extension.zip .

    - name: Upload Artifact
      uses: actions/upload-artifact@v4
      with:
        name: opencampaigns-extension
        path: opencampaigns-extension.zip
